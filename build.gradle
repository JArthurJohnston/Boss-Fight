apply plugin: 'java'
version = '0.0.1'
buildDir = './build'

sourceSets.main.java.srcDirs = ['src']

jar {
    manifest {
        attributes(
                'Main-Class': 'com.paratusSoftware.BossFight'
        )
    }
}
